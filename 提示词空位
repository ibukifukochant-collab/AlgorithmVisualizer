写一个新的插件，名字叫栈，题目是这样的现在可以进行两种操作，

将一个数，从操作数序列的头端移到栈的头端（对应数据结构栈的 push 操作）
将一个数，从栈的头端移到输出序列的尾端（对应数据结构栈的 pop 操作）
使用这两种操作，由一个操作数序列就可以得到一系列的输出序列，下图所示为由 1 2 3 生成序列 2 3 1 的过程。你的程序将对给定的 n，计算并输出由操作数序列 1,2,…,n 经过操作可能得到的输出序列的总数。其中使用动态规划的二维数组进行表示，因为压栈为一个动作，弹栈为另一个动作，此时就可以用二维数组表示，因为必须压一个栈才能弹一个栈，所以不应该存在第0列第1行的存在，这个表格的可视化是以左下角为0，0开始的，所以这个二维数组实际上是一个阶梯状的二维数组，而你需要做这个的可视化，即动态规划的二维数组和栈之间的压栈和弹栈动作，二维数组的可视化则是通过遍历前一位相加，栈的动画则是左边有一个队列，右边有一个队列，而右边的队列会从左边一个一个压紧下面的栈里，每次遍历每一个状态的时候，他都会保存状态，他会把状态储存起来，在动画里的表现就是将此时的栈状态，转换成数组【数字，数字表示】移动到左边的二维数组内，状态转移时，例如3，2，由2，2和3，1转移而来，那么此时动画则是将左边数组的对应方格中的栈状态移动到展示区（栈可视化），并且在上面写下转移方程f[3][2] = f[3][1]+f[2][2],随时变化